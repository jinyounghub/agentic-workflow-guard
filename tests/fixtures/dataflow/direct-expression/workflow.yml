name: Direct expression data flow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  direct:
    runs-on: ubuntu-latest
    steps:
      - id: ai
        uses: openai/codex-action@1111111111111111111111111111111111111111
        with:
          prompt: "Summarize this repository."
      - name: Print AI output
        run: echo "${{ steps.ai.outputs.result }}"
