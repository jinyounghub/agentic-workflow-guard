name: Exclude Path Fixture

on:
  pull_request:

jobs:
  agent:
    runs-on: ubuntu-latest
    steps:
      - uses: openai/codex-action@v1
        with:
          prompt: Summarize this pull request as data only.
