name: Severity Override Fixture

on:
  pull_request:

jobs:
  agent:
    runs-on: ubuntu-latest
    steps:
      - uses: openai/codex-action@1111111111111111111111111111111111111111
        with:
          prompt: Summarize this pull request as data only.
