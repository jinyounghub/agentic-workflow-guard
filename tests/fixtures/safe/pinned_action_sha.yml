name: Safe pinned AI inference

on:
  workflow_dispatch:

permissions:
  contents: read
  models: read

jobs:
  inference:
    runs-on: ubuntu-latest
    steps:
      - name: Run GitHub Models inference
        id: inference
        uses: actions/ai-inference@3333333333333333333333333333333333333333
        with:
          prompt: "Return a short static greeting."
